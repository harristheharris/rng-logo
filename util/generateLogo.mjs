const Square = require('../lib/square');
const Circle = require('../lib/circle');
const Triangle = require('../lib/triangle');

function renderShape(questShape){
    let whatShape = questShape;
    const rngSquare = new Square;
    if (questShape = "Square"){
        return rngSquare
    }


}